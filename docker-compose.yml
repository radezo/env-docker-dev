version: "3"
services:
  webserver:
    build:
      context: ./bin/${PHPVERSION}
    container_name: "${COMPOSE_PROJECT_NAME}-${PHPVERSION}"
    restart: "always"
    ports:
      - "${HOST_MACHINE_UNSECURE_HOST_PORT}:80"
      - "${HOST_MACHINE_SECURE_HOST_PORT}:443"
    links:
      - database
    volumes:
      - ${DOCUMENT_ROOT_DEV1-./www/dev1}:/var/www/dev1:rw
        #- ${DOCUMENT_ROOT_DEV2-./www/dev2}:/var/www/dev2:rw
      - ${PHP_INI-./config/php/php.ini}:/usr/local/etc/php/php.ini
      - ${SSL_DIR-./config/ssl}:/etc/apache2/ssl/
      - ${APACHE_SITES_ENABLED-./config/apache/sites-enabled}:/etc/apache2/sites-enabled
      - ${APACHE_SITES_AVAILABLE-./config/apache/sites-available}:/etc/apache2/sites-available
      - ${LOG_DIR-./logs/apache2}:/var/log/apache2
      - ${XDEBUG_LOG_DIR-./logs/xdebug}:/var/log/xdebug
    environment:
      APACHE_DOCUMENT_ROOT_DEV1: ${APACHE_DOCUMENT_ROOT_DEV1-/var/www/dev1}
        #APACHE_DOCUMENT_ROOT_DEV2: ${APACHE_DOCUMENT_ROOT_DEV2-/var/www/dev2}
      PMA_PORT: ${HOST_MACHINE_PMA_PORT}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      HOST_MACHINE_MYSQL_PORT: ${HOST_MACHINE_MYSQL_PORT}
      XDEBUG_CONFIG: "client_host=host.docker.internal remote_port=${XDEBUG_PORT}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
  database:
    build:
      context: "./bin/${DATABASE}"
    container_name: "${COMPOSE_PROJECT_NAME}-${DATABASE}"
    restart: "always"
    ports:
      - "127.0.0.1:${HOST_MACHINE_MYSQL_PORT}:3306"
    volumes:
      - ${MYSQL_INITDB_DIR-./config/initdb}:/docker-entrypoint-initdb.d
      - ${MYSQL_DATA_DIR-./data/mysql}:/var/lib/mysql
      - ${MYSQL_LOG_DIR-./logs/mysql}:/var/log/mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
        #  phpmyadmin:
        #image: phpmyadmin
        #container_name: "${COMPOSE_PROJECT_NAME}-phpmyadmin"
        #links:
        #- database
        # environment:
  #PMA_HOST: database
  #   PMA_PORT: 3306
  #   PMA_USER: root
  #   PMA_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #   MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #   MYSQL_USER: ${MYSQL_USER}
  #   MYSQL_PASSWORD: ${MYSQL_PASSWORD}
  #   UPLOAD_LIMIT: ${UPLOAD_LIMIT}
  #   MEMORY_LIMIT: ${MEMORY_LIMIT}
  # ports:
  #   - "${HOST_MACHINE_PMA_PORT}:80"
  #   - "${HOST_MACHINE_PMA_SECURE_PORT}:443"
  # volumes:
  #   - /sessions
  #   - ${PHP_INI-./config/php/php.ini}:/usr/local/etc/php/conf.d/php-phpmyadmin.ini
  redis:
    container_name: "${COMPOSE_PROJECT_NAME}-redis"
    image: redis:latest
    ports:
      - "127.0.0.1:${HOST_MACHINE_REDIS_PORT}:6379"
